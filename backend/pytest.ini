[pytest]
testpaths = tests
addopts = -q --cov=app --cov-report=term-missing --cov-report=xml:coverage.xml

markers =
    integration: Tests de integración que usan la DB real y el stack completo de FastAPI.
